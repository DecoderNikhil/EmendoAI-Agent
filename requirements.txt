# Core dependencies
sqlglot>=20.0.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0

# AWS Bedrock (for Claude models via Bedrock)
boto3>=1.34.0
botocore>=1.34.0

# Optional: Anthropic SDK (kept for backwards compatibility)
# anthropic>=0.25.0

# For enhanced logging (optional)
# colorlog>=6.8.0
